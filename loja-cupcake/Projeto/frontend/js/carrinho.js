// Exemplo simples de manipulação do carrinho (a ser adaptado conforme API)

async function adicionarAoCarrinho(produtoId, quantidade) {
  // Lógica para enviar item ao backend e atualizar UI
}

async function carregarCarrinho() {
  // Lógica para buscar e mostrar os itens do carrinho
}

document.getElementById('btn-finalizar')?.addEventListener('click', () => {
  // Chamar API para finalizar compra
});
