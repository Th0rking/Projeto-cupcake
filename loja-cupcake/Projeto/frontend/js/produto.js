// Exemplo simples para listar produtos (a ser adaptado conforme API)

async function listarProdutos() {
  // Fetch dos produtos do backend e criação dinâmica dos cards
}
document.addEventListener('DOMContentLoaded', listarProdutos);
